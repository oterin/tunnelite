# tunnel node configuration
# this is the address the node's own api listens on.
# used by the register_node.py script to talk to the node locally.
NODE_PUBLIC_ADDRESS="http://127.0.0.1:8001"

# this is the base url of the main server api.
# the node will append paths like '/api/nodes/register' to this url.
MAIN_SERVER_URL="http://127.0.0.1:8000"
